# Cluster 45

def spectral_norm(module, mode=True):
    if mode:
        return _spectral_norm(module)
    return module

def spectral_norm(module, mode=True):
    if mode:
        return _spectral_norm(module)
    return module

