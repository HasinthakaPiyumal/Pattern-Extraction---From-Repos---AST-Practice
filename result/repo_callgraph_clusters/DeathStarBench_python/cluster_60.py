# Cluster 60

def print_info_resp(video_info: dict):
    for vid in video_info:
        print_info(vid, video_info[vid])

# Node: print_info
def print_info_resp(video_info: dict):
    for vid in video_info:
        print_info(vid, video_info[vid])

